{
  "name": "playwright-composition-reference",
  "version": "1.0.0",
  "description": "Playwright automation framework using Mixin Design Pattern for composition over inheritance",
  "author": "Steven Granados",
  "license": "ISC",
  "scripts": {
    "test": "npx playwright test",
    "test:flake": "npx playwright test --repeat-each=20 --retries=0",
    "test:flake:clone-published": "npx playwright test tests/clone-published-campaign.spec.ts --repeat-each=20 --retries=0",
    "test:flake:report": "npm run test:flake && npm run allure:generate:history",
    "test:headed": "npx playwright test --headed",
    "test:ui": "npx playwright test --ui",
    "test:debug": "npx playwright test --debug",
    "allure:prepare-history": "node scripts/allure-prepare-history.mjs",
    "allure:generate": "allure generate allure-results --clean -o allure-report",
    "allure:generate:history": "npm run allure:prepare-history && npm run allure:generate",
    "allure:open": "allure open allure-report",
    "allure:serve": "allure serve allure-results",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "@typescript-eslint/eslint-plugin": "^8.17.0",
    "@typescript-eslint/parser": "^8.17.0",
    "allure-commandline": "^2.32.0",
    "allure-js-commons": "^3.0.7",
    "allure-playwright": "^3.0.7",
    "dotenv": "^16.4.7",
    "eslint": "^9.16.0",
    "mailinator-client": "^1.1.0",
    "typescript": "^5.7.2",
    "winston": "^3.17.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
